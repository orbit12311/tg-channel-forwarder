# From https://my.telegram.org -> API development tools
API_ID=12345
API_HASH=your_api_hash_here

# Generated once via `npm run login` (see generate-session.js)
SESSION_STRING=

# Channel you're a member of (not admin) - username without @, or -100... id
SOURCE_CHANNEL=source_channel_username

# Channel you admin - username without @, or -100... id
DEST_CHANNEL=your_channel_username

# true = keeps "Forwarded from" tag, false = clean re-post
KEEP_FORWARD_TAG=true
